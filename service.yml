name: monitoring-kube-state-metrics
title: Monitoring kube-state-metrics
type: infrastructure
scalable: false
containers:
  - name: kube-state-metrics
    resources:
      request:
        cpu: 50
        memory: 80
      limit:
        cpu: 200
        memory: 200
